{
	"name": "obsidian-proxy-server",
	"version": "0.1.0",
	"description": "Proxy server for mobile Obsidian plugin to use Claude Code on desktop",
	"type": "module",
	"main": "dist/server.js",
	"scripts": {
		"build": "tsc",
		"start": "node dist/server.js",
		"dev": "tsc --watch"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.5.0"
	}
}
