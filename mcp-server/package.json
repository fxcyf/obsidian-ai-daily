{
	"name": "obsidian-vault-mcp",
	"version": "0.1.0",
	"description": "MCP server for Obsidian vault operations — use with Claude Code",
	"type": "module",
	"main": "dist/index.js",
	"bin": {
		"obsidian-vault-mcp": "dist/index.js"
	},
	"scripts": {
		"build": "tsc",
		"start": "node dist/index.js",
		"dev": "tsc --watch",
		"test": "vitest run"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.1"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.5.0",
		"vitest": "^2.0.0"
	}
}
